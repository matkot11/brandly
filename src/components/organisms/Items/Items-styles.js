import styled from 'styled-components';

export const StyledWrapper = styled.div`
  width: 75vw;
  justify-self: center;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;

  & > * {
    margin: 3rem;
  }
`;
